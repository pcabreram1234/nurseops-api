import { Injectable } from "@nestjs/common";

@Injectable()
export class SpecialityRankingService {
  calculateScore(nurse: any) {
    /*
    |--------------------------------------------------------------------------
    | FUTURE AI SCORING
    |--------------------------------------------------------------------------
    */

    return 0.95;
  }
}
