import { Injectable } from "@nestjs/common";

@Injectable()
export class TemplateMatchingService {
  match(nurse: any, template: any) {
    /*
    |--------------------------------------------------------------------------
    | FUTURE AI MATCHING
    |--------------------------------------------------------------------------
    */

    return {
      compatible: true,
    };
  }
}
